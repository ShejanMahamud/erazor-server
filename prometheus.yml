global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'nestjs'
    static_configs:
      - targets: ['localhost:4545']
        labels:
          app: erazor-ai
    metrics_path: '/v1/api/metrics'
