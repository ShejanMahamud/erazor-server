language: node_js
node_js:
  - '18'
  - '20'

cache:
  directories:
    - ~/.pnpm-store

install:
  - npm install -g pnpm
  - pnpm install --frozen-lockfile

script:
  - pnpm test
  - pnpm build

branches:
  only:
    - main
    - develop

notifications:
  email: false
